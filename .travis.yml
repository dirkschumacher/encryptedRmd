# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
dist: xenial
latex: false
fortran: false

addons:
  apt:
    packages:
      - libsodium-dev
